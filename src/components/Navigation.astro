---
---
<div id="navbar" class="nav-links">
    <a class="navlink" href="/#intro">Kezdőlap</a>
    <a class="navlink" href="/rolam#rolam">Rólam</a>
    <a class="navlink" href="/szolgaltatasok#szolgaltatasok">Szolgáltatások</a>
    <a class="navlink" href="#kapcsolat">Kapcsolat</a>
</div>

<style>

/* .nav-links {
    display: none;
    position: absolute;
    top: 60px;
    padding: 20px 200px 20px 20px;
    background: linear-gradient(90deg, rgba(255, 250, 250, 1) 0%, rgba(255, 250, 250, 1) 50%, rgba(255, 250, 250, 0) 100%);
    opacity: 0.9;
}

.nav-links a{
    display: block;
    color:  #6d5040;
    text-transform: uppercase;
    text-decoration: none;
    font-weight: 600;
    line-height: 40px;
}

.nav-links a:hover,
.nav-links a:focus {
    color: #946d57;
} */

@media screen and (min-width: 1100px) {
      
      /* .nav-links {
          margin-left: 5em;
          display: block;
          position: absolute;
          top: 8px;
          left: 16px;
          width: auto;
          background: none;
      }
  
      .nav-links a {
          display: inline-block;
          padding: 15px 20px;
      } */

    /* Style the navbar */

    #navbar {
        position: fixed;
        overflow: hidden;
        background-color: #333333e0;
        z-index: 100;
        width: 100%;
    }

    /* Navbar links */
    #navbar a {
        float: left;
        display: block;
        color: #f2f2f2;
        text-align: center;
        padding: 14px;
        text-decoration: none;
    }

    /* Page content */
    .content {
        padding: 16px;
    }

    .hide {
        display: none;
    }   

    /* The sticky class is added to the navbar with JS when it reaches its scroll position */
    .sticky {
        position: fixed;
        top: 0;
        width: 100%;
    }

    /* Add some top padding to the page content to prevent sudden quick movement (as the navigation bar gets a new position at the top of the page (position:fixed and top:0) */
    .sticky + .content {
        padding-top: 60px;
    }
}
</style>

<script>
// When the user scrolls the page, execute myFunction
window.onscroll = function() {hideOnScroll()};

var hiding = false;
hideIfNotOnTopTimeout();

// Get the navbar
var navbar = document.getElementById("navbar");


var links = document.getElementsByClassName("navlink");

for (let link of links) {
    link.addEventListener("click", function() {
        navbar.classList.add("hide");
    });
}   

// Check if page is on top

// Get the offset position of the navbar
// var sticky = navbar.offsetTop;

// Add the sticky class to the navbar when you reach its scroll position. Remove "sticky" when you leave the scroll position
let prevPos = window.scrollY;

function hideOnScroll() {
    console.log("scrolling");
    let currentPos = window.scrollY;
    if (currentPos < prevPos) {
        navbar.classList.remove("hide");
    }
    prevPos = currentPos;

    hideIfNotOnTopTimeout();
}

function hideIfNotOnTopTimeout() {
    hiding = true;
    setTimeout(() => {
    if (window.scrollY !== 0) {
        console.log("hide");
        navbar.classList.add("hide");
        hiding = false;
    }
    }, 2000);
}
</script>